# requirements.txt
# Vercel reads this from the project root to install dependencies
# for all Python serverless functions.

# Database (async PostgreSQL)
sqlalchemy==2.0.30
asyncpg==0.29.0

# Authentication
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-jose[cryptography]==3.3.0

# Email validation (used in login parsing)
email-validator==2.1.1
