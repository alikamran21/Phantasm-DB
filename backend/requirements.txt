# backend/requirements.txt
# Pin to exact minor versions for reproducible builds.
# Run: pip install -r requirements.txt

# --- Web framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0          # ASGI server with uvloop + httptools

# --- Database (async PostgreSQL) ---
sqlalchemy==2.0.30
asyncpg==0.29.0                    # Async PostgreSQL driver
alembic==1.13.1                    # Schema migrations (optional; init_db() used for MVP)

# --- Authentication & Cryptography ---
passlib[bcrypt]==1.7.4             # Password hashing
bcrypt==4.1.3                      # Explicit bcrypt backend
python-jose[cryptography]==3.3.0   # JWT (HS256 / RS256)

# --- Validation ---
pydantic[email]==2.7.1             # Request body validation + email validator
email-validator==2.1.1

# --- HTTP utilities ---
httpx==0.27.0                      # Async HTTP client (testing, webhooks)
python-multipart==0.0.9            # Form data parsing

# --- Environment ---
python-dotenv==1.0.1               # Load .env in development

# --- Observability ---
structlog==24.1.0                  # Structured logging (optional enhancement)
